export type Theme = {
  colors: {
    background: string;
    foreground: string;
    surface: string;
    surfaceForeground: string;
    muted: string;
    border: string;
    primary: string;
    primaryHover: string;
    primaryForeground: string;
  };

  radius: {
    button: string;
    card: string;
  };
};